{
  "cause": "TEST_PATTERN_MISMATCH",
  "explanation": "The test runner failed to execute because the file path or test filter argument passed to the CLI did not match any test files, often caused by unescaped special characters (like $ or *) or an incorrect file naming pattern.",
  "fixedCode": "const { test, expect } = require('@playwright/test');\n\ntest('loads data reliably', async ({ page }) => {\n  await page.goto('http://localhost:8080/flaky.html');\n  await page.click('#loadBtn');\n  await expect(page.locator('#result')).toHaveText('Data Loaded');\n});"
}