{
  "cause": "RACE_CONDITION",
  "explanation": "The test uses page.textContent() immediately after clicking the button, which retrieves the DOM text at that exact millisecond without waiting for asynchronous data fetching or rendering to complete. Using Playwright's auto-retrying locator assertion page.locator().toHaveText() retries until the expected text appears or times out.",
  "fixedCode": "const { test, expect } = require('@playwright/test');\n\ntest('loads data on button click', async ({ page }) => {\n  await page.goto('http://localhost:8080/flaky.html');\n  await page.click('#loadBtn');\n  await expect(page.locator('#result')).toHaveText('Data Loaded');\n});"
}